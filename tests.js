const should = require('chai').should();
const solution = require('solution');

describe('should do a lot of parsing thing', () =>{
    it('should console log JSON string',() => {
        console.log()
    });
})